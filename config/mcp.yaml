# MCP Server 配置
# 每个 server 通过 stdio 子进程启动，使用 JSON-RPC 2.0 通信。
# 环境变量支持 ${VAR} 语法展开。

servers:
  # 示例：文件系统访问（需要 npx）
  # - name: filesystem
  #   command: npx
  #   args: ["-y", "@modelcontextprotocol/server-filesystem", "D:/"]
  #   enabled: true

  # 示例：GitHub（需要 token）
  # - name: github
  #   command: npx
  #   args: ["-y", "@modelcontextprotocol/server-github"]
  #   env:
  #     GITHUB_TOKEN: "${GITHUB_TOKEN}"
  #   enabled: true

  # 示例：SQLite 数据库
  # - name: sqlite
  #   command: npx
  #   args: ["-y", "@modelcontextprotocol/server-sqlite", "./data.db"]
  #   enabled: true
